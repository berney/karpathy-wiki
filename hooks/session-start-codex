#!/usr/bin/env bash
# SessionStart hook for karpathy-wiki plugin (Codex)
# No session-wide bootstrap needed — each skill is self-contained.

set -euo pipefail

printf '{"hookSpecificOutput": {"hookEventName": "SessionStart"}}\n' | cat
exit 0
