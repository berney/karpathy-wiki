# karpathy-wiki for Pi

This plugin provides skills for managing a twillm wiki — setup, daily operations, health checks, and description generation.

To install, add karpathy-wiki as a git dependency in your Pi config, or clone this repo into your Pi plugins directory.

Skills are loaded from the `skills/` directory using standard SKILL.md format compatible with Pi's skill system.
